apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "motivator-service.fullname" . }}-test-connection"
  labels:
    {{- include "motivator-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "motivator-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
